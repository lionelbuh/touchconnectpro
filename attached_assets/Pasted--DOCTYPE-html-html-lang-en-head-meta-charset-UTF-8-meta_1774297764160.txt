
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Join as a Specialist - TouchConnectPro</title>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,wght@0,300;0,400;0,600;1,300;1,400&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    --cream: #FAF8F3;
    --ink: #1A1814;
    --ink-soft: #4A4740;
    --ink-muted: #8C8880;
    --gold: #C49A3C;
    --gold-light: #F0E4C4;
    --gold-pale: #FAF3E0;
    --teal: #1D6A5A;
    --teal-light: #E4F0ED;
    --border: rgba(26,24,20,0.12);
    --border-soft: rgba(26,24,20,0.07);
    --serif: 'Fraunces', Georgia, serif;
    --sans: 'DM Sans', system-ui, sans-serif;
    --radius: 4px;
    --radius-lg: 10px;
  }
  html { scroll-behavior: smooth; }
  body {
    font-family: var(--sans);
    background: var(--cream);
    color: var(--ink);
    font-size: 16px;
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
  }

  /* NAV */
  nav {
    position: sticky; top: 0; z-index: 100;
    background: var(--cream);
    border-bottom: 1px solid var(--border-soft);
    padding: 0 2rem;
    display: flex; align-items: center; justify-content: space-between;
    height: 64px;
  }
  .nav-logo {
    font-family: var(--serif);
    font-weight: 600;
    font-size: 17px;
    color: var(--ink);
    text-decoration: none;
    letter-spacing: -0.01em;
  }
  .nav-logo span { color: var(--gold); }
  .nav-links { display: flex; align-items: center; gap: 2rem; }
  .nav-links a {
    font-size: 13px;
    color: var(--ink-soft);
    text-decoration: none;
    transition: color 0.15s;
  }
  .nav-links a:hover { color: var(--ink); }
  .btn-nav {
    font-size: 13px;
    font-weight: 500;
    color: var(--cream) !important;
    background: var(--ink);
    padding: 8px 18px;
    border-radius: var(--radius);
    text-decoration: none;
    transition: background 0.2s;
  }
  .btn-nav:hover { background: var(--teal) !important; }

  /* HERO */
  .hero {
    max-width: 1060px;
    margin: 0 auto;
    padding: 88px 2rem 72px;
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: 80px;
    align-items: center;
  }
  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--teal);
    background: var(--teal-light);
    padding: 6px 14px;
    border-radius: 100px;
    margin-bottom: 24px;
  }
  .hero-eyebrow::before {
    content: '';
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--teal);
    display: inline-block;
  }
  .hero h1 {
    font-family: var(--serif);
    font-size: clamp(34px, 4.5vw, 52px);
    font-weight: 300;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: 20px;
  }
  .hero h1 em { font-style: italic; color: var(--gold); }
  .hero-sub {
    font-size: 17px;
    color: var(--ink-soft);
    line-height: 1.75;
    max-width: 460px;
    margin-bottom: 36px;
  }
  .btn-primary {
    display: inline-block;
    background: var(--ink);
    color: var(--cream);
    font-size: 14px;
    font-weight: 500;
    padding: 14px 30px;
    border-radius: var(--radius);
    text-decoration: none;
    transition: background 0.2s, transform 0.15s;
  }
  .btn-primary:hover { background: var(--teal); transform: translateY(-1px); }
  .hero-note { margin-top: 14px; font-size: 12px; color: var(--ink-muted); }

  /* HERO CARD */
  .hero-card {
    background: var(--ink);
    border-radius: var(--radius-lg);
    padding: 32px;
  }
  .hero-card-label {
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(250,248,243,0.35);
    margin-bottom: 20px;
    display: block;
  }
  .lead-item {
    display: flex;
    gap: 14px;
    padding: 14px 0;
    border-bottom: 1px solid rgba(255,255,255,0.06);
    align-items: flex-start;
  }
  .lead-item:last-of-type { border-bottom: none; }
  .lead-score {
    width: 40px; height: 40px;
    border-radius: var(--radius);
    background: rgba(255,255,255,0.05);
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  .lead-score-num {
    font-family: var(--serif);
    font-size: 16px;
    font-weight: 300;
    color: var(--cream);
    line-height: 1;
  }
  .lead-score-label { font-size: 9px; color: rgba(250,248,243,0.3); letter-spacing: 0.05em; }
  .lead-info { flex: 1; }
  .lead-name { font-size: 13px; font-weight: 500; color: var(--cream); margin-bottom: 3px; }
  .lead-gap {
    display: inline-block;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 2px 8px;
    border-radius: 100px;
  }
  .gap-finance { color: #C97B5A; background: rgba(201,123,90,0.15); }
  .gap-legal { color: var(--gold); background: rgba(196,154,60,0.15); }
  .gap-ops { color: var(--teal); background: rgba(29,106,90,0.2); }
  .gap-pitch { color: #9B7FD4; background: rgba(155,127,212,0.15); }
  .lead-stage { font-size: 11px; color: rgba(250,248,243,0.35); margin-top: 3px; }
  .hero-card-foot {
    margin-top: 20px;
    padding-top: 16px;
    border-top: 1px solid rgba(255,255,255,0.06);
    font-size: 12px;
    color: rgba(250,248,243,0.3);
    line-height: 1.5;
  }

  /* HOW IT WORKS */
  .how-wrap {
    background: var(--ink);
    padding: 80px 2rem;
  }
  .how-inner { max-width: 900px; margin: 0 auto; }
  .section-eyebrow {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(250,248,243,0.35);
    margin-bottom: 16px;
    display: block;
  }
  .how-inner h2 {
    font-family: var(--serif);
    font-size: clamp(26px, 3.5vw, 38px);
    font-weight: 300;
    color: var(--cream);
    margin-bottom: 12px;
    line-height: 1.2;
  }
  .how-inner h2 em { font-style: italic; color: var(--gold); }
  .how-inner > p {
    font-size: 15px;
    color: rgba(250,248,243,0.5);
    max-width: 520px;
    line-height: 1.7;
    margin-bottom: 56px;
  }
  .how-steps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.06);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }
  .how-step { padding: 32px 28px; background: var(--ink); }
  .step-num {
    font-family: var(--serif);
    font-size: 28px;
    font-weight: 300;
    color: rgba(250,248,243,0.15);
    margin-bottom: 14px;
    line-height: 1;
  }
  .how-step h3 {
    font-family: var(--serif);
    font-size: 17px;
    font-weight: 400;
    color: var(--cream);
    margin-bottom: 10px;
    line-height: 1.3;
  }
  .how-step p { font-size: 13px; color: rgba(250,248,243,0.45); line-height: 1.65; }

  /* SPECIALISMS */
  .specialisms-wrap {
    max-width: 900px;
    margin: 0 auto;
    padding: 88px 2rem;
  }
  .specialisms-header { margin-bottom: 40px; }
  .specialisms-header h2 {
    font-family: var(--serif);
    font-size: clamp(26px, 3.5vw, 38px);
    font-weight: 300;
    color: var(--ink);
    line-height: 1.2;
    margin-bottom: 12px;
  }
  .specialisms-header h2 em { font-style: italic; color: var(--gold); }
  .specialisms-header p { font-size: 15px; color: var(--ink-soft); max-width: 500px; line-height: 1.7; }
  .specialism-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 14px;
  }
  .specialism-card {
    background: white;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 24px 22px;
  }
  .specialism-icon {
    width: 36px; height: 36px;
    border-radius: var(--radius);
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 14px;
  }
  .specialism-icon svg { width: 18px; height: 18px; stroke: currentColor; fill: none; stroke-width: 1.5; }
  .icon-finance { background: rgba(29,106,90,0.1); color: var(--teal); }
  .icon-legal   { background: rgba(196,154,60,0.12); color: #7A5C1E; }
  .icon-mktg    { background: rgba(155,127,212,0.12); color: #5A3FA0; }
  .icon-tech    { background: rgba(26,24,20,0.06); color: var(--ink-soft); }
  .icon-pitch   { background: rgba(201,123,90,0.1); color: #8B4A2A; }
  .specialism-card h3 {
    font-family: var(--serif);
    font-size: 16px;
    font-weight: 400;
    color: var(--ink);
    margin-bottom: 8px;
    line-height: 1.3;
  }
  .specialism-card p { font-size: 13px; color: var(--ink-muted); line-height: 1.65; }
  .specialism-tag {
    display: inline-block;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: var(--teal);
    background: var(--teal-light);
    padding: 3px 9px;
    border-radius: 100px;
    margin-top: 12px;
  }
  .specialism-tag.open { color: #8B4A2A; background: rgba(201,123,90,0.1); }

  /* WHAT YOU GET */
  .benefits-wrap {
    background: var(--gold-pale);
    border-top: 1px solid rgba(196,154,60,0.2);
    border-bottom: 1px solid rgba(196,154,60,0.2);
    padding: 80px 2rem;
  }
  .benefits-inner { max-width: 900px; margin: 0 auto; }
  .benefits-inner h2 {
    font-family: var(--serif);
    font-size: clamp(26px, 3.5vw, 36px);
    font-weight: 300;
    color: var(--ink);
    margin-bottom: 40px;
    line-height: 1.2;
  }
  .benefits-inner h2 em { font-style: italic; }
  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }
  .benefit-item {
    display: flex;
    gap: 14px;
    align-items: flex-start;
  }
  .benefit-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--gold);
    flex-shrink: 0;
    margin-top: 6px;
  }
  .benefit-item h3 { font-size: 14px; font-weight: 500; color: var(--ink); margin-bottom: 4px; }
  .benefit-item p { font-size: 13px; color: var(--ink-soft); line-height: 1.6; }

  /* EARNINGS */
  .earnings-wrap {
    max-width: 900px;
    margin: 0 auto;
    padding: 88px 2rem;
  }
  .earnings-wrap h2 {
    font-family: var(--serif);
    font-size: clamp(26px, 3.5vw, 36px);
    font-weight: 300;
    color: var(--ink);
    margin-bottom: 12px;
    line-height: 1.2;
  }
  .earnings-wrap h2 em { font-style: italic; color: var(--gold); }
  .earnings-wrap > p {
    font-size: 15px;
    color: var(--ink-soft);
    max-width: 520px;
    line-height: 1.75;
    margin-bottom: 36px;
  }
  .earnings-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 14px;
    margin-bottom: 24px;
  }
  .earnings-card {
    background: white;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 22px 20px;
    text-align: center;
  }
  .earnings-card .label { font-size: 11px; color: var(--ink-muted); letter-spacing: 0.06em; text-transform: uppercase; margin-bottom: 8px; display: block; }
  .earnings-card .value {
    font-family: var(--serif);
    font-size: 28px;
    font-weight: 300;
    color: var(--ink);
    letter-spacing: -0.02em;
  }
  .earnings-card .sub { font-size: 12px; color: var(--ink-muted); margin-top: 4px; }
  .earnings-note {
    padding: 16px 20px;
    background: var(--teal-light);
    border-radius: var(--radius-lg);
    font-size: 14px;
    color: var(--teal);
    font-weight: 500;
    line-height: 1.6;
  }

  /* APPLY */
  .apply-wrap {
    background: var(--ink);
    padding: 88px 2rem;
    text-align: center;
  }
  .apply-inner { max-width: 600px; margin: 0 auto; }
  .apply-inner h2 {
    font-family: var(--serif);
    font-size: clamp(28px, 4vw, 42px);
    font-weight: 300;
    color: var(--cream);
    line-height: 1.15;
    margin-bottom: 16px;
  }
  .apply-inner h2 em { font-style: italic; color: var(--gold); }
  .apply-inner p {
    font-size: 16px;
    color: rgba(250,248,243,0.55);
    line-height: 1.75;
    margin-bottom: 40px;
  }
  .apply-form { text-align: left; }
  .form-row { margin-bottom: 14px; }
  .form-label {
    display: block;
    font-size: 12px;
    font-weight: 500;
    color: rgba(250,248,243,0.5);
    margin-bottom: 6px;
    letter-spacing: 0.02em;
  }
  .form-input, .form-select, .form-textarea {
    width: 100%;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: var(--radius);
    font-family: var(--sans);
    font-size: 14px;
    color: var(--cream);
    outline: none;
    transition: border-color 0.15s;
    appearance: none;
  }
  .form-input, .form-select { height: 44px; padding: 0 14px; }
  .form-textarea { padding: 12px 14px; resize: vertical; min-height: 90px; line-height: 1.5; }
  .form-input::placeholder, .form-textarea::placeholder { color: rgba(250,248,243,0.25); }
  .form-input:focus, .form-select:focus, .form-textarea:focus { border-color: rgba(196,154,60,0.5); }
  .form-select option { background: var(--ink); color: var(--cream); }
  .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
  .btn-apply {
    width: 100%;
    height: 48px;
    background: var(--gold);
    color: var(--ink);
    font-family: var(--sans);
    font-size: 14px;
    font-weight: 500;
    border: none;
    border-radius: var(--radius);
    cursor: pointer;
    margin-top: 8px;
    transition: opacity 0.15s, transform 0.1s;
    letter-spacing: 0.01em;
  }
  .btn-apply:hover { opacity: 0.88; transform: translateY(-1px); }
  .apply-note { margin-top: 14px; font-size: 12px; color: rgba(250,248,243,0.25); text-align: center; line-height: 1.6; }

  /* FOOTER */
  .footer-wrap {
    border-top: 1px solid var(--border-soft);
    padding: 40px 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: 1100px;
    margin: 0 auto;
  }
  .footer-logo { font-family: var(--serif); font-size: 15px; color: var(--ink-muted); }
  .footer-logo span { color: var(--gold); }
  .footer-wrap p { font-size: 12px; color: var(--ink-muted); }

  @media (max-width: 860px) {
    .hero { grid-template-columns: 1fr; gap: 48px; padding: 60px 1.5rem 48px; }
    .how-steps { grid-template-columns: 1fr; }
    .specialism-grid { grid-template-columns: repeat(2, 1fr); }
    .earnings-cards { grid-template-columns: 1fr; }
    .benefits-grid { grid-template-columns: 1fr; }
    .form-grid { grid-template-columns: 1fr; }
    .nav-links { display: none; }
    .footer-wrap { flex-direction: column; gap: 12px; text-align: center; }
  }
  @media (max-width: 560px) {
    .specialism-grid { grid-template-columns: 1fr; }
  }
</style>
</head>
<body>

<nav>
  <a href="index.html" class="nav-logo">Touch<span>Connect</span>Pro</a>
  <div class="nav-links">
    <a href="how-it-works.html">How it works</a>
    <a href="insights.html">Insights</a>
    <a href="focus-score.html" class="btn-nav">Take the free score</a>
  </div>
</nav>

<!-- HERO -->
<section>
  <div class="hero">
    <div>
      <div class="hero-eyebrow">Join as a specialist</div>
      <h1>The founders are already here. They need <em>your expertise.</em></h1>
      <p class="hero-sub">TouchConnectPro sends you pre-qualified founders whose diagnostic results show a specific gap in your area. No cold outreach. No client chasing. You show up and help.</p>
      <a href="#apply" class="btn-primary">Apply to join the network</a>
      <p class="hero-note">We review every application. You hear back within a week.</p>
    </div>

    <div class="hero-card">
      <span class="hero-card-label">Founders waiting for a specialist</span>
      <div class="lead-item">
        <div class="lead-score">
          <div class="lead-score-num">41</div>
          <div class="lead-score-label">finance</div>
        </div>
        <div class="lead-info">
          <div class="lead-name">Sarah K.</div>
          <span class="lead-gap gap-finance">Financial structure gap</span>
          <div class="lead-stage">Post-revenue, no accounting system</div>
        </div>
      </div>
      <div class="lead-item">
        <div class="lead-score">
          <div class="lead-score-num">38</div>
          <div class="lead-score-label">legal</div>
        </div>
        <div class="lead-info">
          <div class="lead-name">Marcus T.</div>
          <span class="lead-gap gap-legal">Legal readiness gap</span>
          <div class="lead-stage">Pre-raise, no entity structure</div>
        </div>
      </div>
      <div class="lead-item">
        <div class="lead-score">
          <div class="lead-score-num">45</div>
          <div class="lead-score-label">pitch</div>
        </div>
        <div class="lead-info">
          <div class="lead-name">Priya M.</div>
          <span class="lead-gap gap-pitch">Pitch readiness gap</span>
          <div class="lead-stage">Seed stage, investor meetings in 6 weeks</div>
        </div>
      </div>
      <div class="lead-item">
        <div class="lead-score">
          <div class="lead-score-num">36</div>
          <div class="lead-score-label">ops</div>
        </div>
        <div class="lead-info">
          <div class="lead-name">James R.</div>
          <span class="lead-gap gap-ops">Operational gap</span>
          <div class="lead-stage">Scaling, no systems in place</div>
        </div>
      </div>
      <div class="hero-card-foot">Founders are matched to specialists based on their Focus Score gap. You only receive leads in your area.</div>
    </div>
  </div>
</section>

<!-- HOW IT WORKS -->
<section class="how-wrap">
  <div class="how-inner">
    <span class="section-eyebrow">How it works</span>
    <h2>You focus on the work. <em>We handle the rest.</em></h2>
    <p>The platform qualifies founders, identifies their specific gap, and routes them to the right specialist. Your job is to show up and deliver.</p>
    <div class="how-steps">
      <div class="how-step">
        <div class="step-num">01</div>
        <h3>Founder takes the diagnostic</h3>
        <p>Every founder on the platform completes the Founder Focus Score, an 8-question diagnostic that identifies their exact gap across financial structure, operations, and clarity.</p>
      </div>
      <div class="how-step">
        <div class="step-num">02</div>
        <h3>Their gap matches your expertise</h3>
        <p>When a founder's score reveals a gap in your area, a notification is sent. You see their score, their stage, and their 90-day goal before you respond to a single message.</p>
      </div>
      <div class="how-step">
        <div class="step-num">03</div>
        <h3>You take it from there</h3>
        <p>You start with a free conversation in the platform. If there is a fit, you scope and agree an engagement directly. You set the terms. We handle the introduction.</p>
      </div>
    </div>
  </div>
</section>

<!-- SPECIALISMS -->
<section class="specialisms-wrap">
  <div class="specialisms-header">
    <span style="font-size:11px; font-weight:500; letter-spacing:0.12em; text-transform:uppercase; color:var(--ink-muted); margin-bottom:14px; display:block;">Who we are looking for</span>
    <h2>Five areas where founders <em>need the most help</em></h2>
    <p>These are the gaps that come up most consistently in our diagnostic data. If your expertise maps to any of them, there are founders here who need you right now.</p>
  </div>
  <div class="specialism-grid">
    <div class="specialism-card">
      <div class="specialism-icon icon-finance">
        <svg viewBox="0 0 24 24"><path d="M12 2v20M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/></svg>
      </div>
      <h3>Accounting and ERP</h3>
      <p>Founders who have outgrown spreadsheets and need accounting systems, ERP selection, financial reporting, or fractional CFO thinking.</p>
      <span class="specialism-tag">Actively recruiting</span>
    </div>
    <div class="specialism-card">
      <div class="specialism-icon icon-legal">
        <svg viewBox="0 0 24 24"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
      </div>
      <h3>Legal for startups</h3>
      <p>Entity structure, founder agreements, IP protection, employment basics, and contract review for founders who are building without legal clarity.</p>
      <span class="specialism-tag open">Open position</span>
    </div>
    <div class="specialism-card">
      <div class="specialism-icon icon-mktg">
        <svg viewBox="0 0 24 24"><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></svg>
      </div>
      <h3>Marketing and growth</h3>
      <p>Go-to-market strategy, positioning, early customer acquisition, and growth systems for founders who have a product but no repeatable traction.</p>
      <span class="specialism-tag open">Open position</span>
    </div>
    <div class="specialism-card">
      <div class="specialism-icon icon-tech">
        <svg viewBox="0 0 24 24"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
      </div>
      <h3>Tech and product</h3>
      <p>Technical architecture decisions, build vs buy tradeoffs, product roadmap prioritization, and dev team structure for non-technical founders.</p>
      <span class="specialism-tag open">Open position</span>
    </div>
    <div class="specialism-card">
      <div class="specialism-icon icon-pitch" style="background:rgba(201,123,90,0.1); color:#8B4A2A;">
        <svg viewBox="0 0 24 24"><path d="M2 3h6a4 4 0 014 4v14a3 3 0 00-3-3H2z"/><path d="M22 3h-6a4 4 0 00-4 4v14a3 3 0 013-3h7z"/></svg>
      </div>
      <h3>Pitch and fundraising</h3>
      <p>Pitch deck structure, investor narrative, fundraising strategy, and presentation coaching for founders preparing for their first or next raise.</p>
      <span class="specialism-tag open">Open position</span>
    </div>
  </div>
</section>

<!-- WHAT YOU GET -->
<section class="benefits-wrap">
  <div class="benefits-inner">
    <h2>What joining the network <em>actually means</em></h2>
    <div class="benefits-grid">
      <div class="benefit-item">
        <div class="benefit-dot"></div>
        <div>
          <h3>Pre-qualified leads, not cold inquiries</h3>
          <p>Every founder you speak to has already completed the diagnostic. You know their stage, their gap, and their 90-day goal before the first message.</p>
        </div>
      </div>
      <div class="benefit-item">
        <div class="benefit-dot"></div>
        <div>
          <h3>You set your own terms</h3>
          <p>Scope, price, and format are agreed directly between you and the founder. We do not dictate how you work or what you charge.</p>
        </div>
      </div>
      <div class="benefit-item">
        <div class="benefit-dot"></div>
        <div>
          <h3>No client chasing</h3>
          <p>You do not market yourself, manage a pipeline, or follow up cold leads. The platform handles the matching. You focus on the work.</p>
        </div>
      </div>
      <div class="benefit-item">
        <div class="benefit-dot"></div>
        <div>
          <h3>Work with founders who are serious</h3>
          <p>The diagnostic filters out curiosity. Founders who reach out have identified a real gap and are looking for someone to help them fix it.</p>
        </div>
      </div>
      <div class="benefit-item">
        <div class="benefit-dot"></div>
        <div>
          <h3>Build your reputation in a focused niche</h3>
          <p>Your specialist profile is visible to founders whose gap matches your area. Over time, your track record on the platform speaks for itself.</p>
        </div>
      </div>
      <div class="benefit-item">
        <div class="benefit-dot"></div>
        <div>
          <h3>Flexible engagement, no minimum commitment</h3>
          <p>Take on as much or as little as fits your schedule. There is no minimum number of clients, no mandatory availability, and no lock-in.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- EARNINGS -->
<section class="earnings-wrap">
  <span style="font-size:11px; font-weight:500; letter-spacing:0.12em; text-transform:uppercase; color:var(--ink-muted); margin-bottom:14px; display:block;">Revenue structure</span>
  <h2>You set the price. <em>You keep 80%.</em></h2>
  <p>We take a 20% platform fee to operate TouchConnectPro and keep the leads coming. No subscription, no listing fee, no charge until you earn.</p>
  <div class="earnings-cards">
    <div class="earnings-card">
      <span class="label">Your share</span>
      <div class="value">80%</div>
      <div class="sub">of every engagement</div>
    </div>
    <div class="earnings-card">
      <span class="label">Platform fee</span>
      <div class="value">20%</div>
      <div class="sub">only when you earn</div>
    </div>
    <div class="earnings-card">
      <span class="label">Listing cost</span>
      <div class="value">$0</div>
      <div class="sub">free to join and be listed</div>
    </div>
  </div>
  <div class="earnings-note">No subscription. No pay-to-be-listed. We only earn when you do.</div>
</section>